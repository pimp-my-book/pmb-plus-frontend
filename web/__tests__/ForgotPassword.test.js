import { render, fireEvent, getByLabelText } from '@testing-library/react'

describe('<ForgotPassword/>', () => {

    it('Renders the Request Form First', () => {

    })

    it('is able to submit the request', () => {

    })

    it('is able to set a new password and render confirmation screen', () => {

    })


})
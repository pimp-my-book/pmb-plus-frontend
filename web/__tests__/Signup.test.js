import { render, fireEvent, getByLabelText } from '@testing-library/react'

describe('<Signup/>', () => {
    it('Allows the user to sign up', () => {

    })

    it('Does not allow form to be submitted if fields not validated', () => {

    })

    it('Shows an error if something went wrong', () => {

    })
})